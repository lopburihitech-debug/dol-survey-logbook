# MVP นี้ตั้งใจใช้เฉพาะไลบรารีน้ำหนักเบาเพื่อให้ "รันได้ทันที" โดยไม่ต้องพึ่งบริการภายนอก
Flask==3.1.3
PyJWT==2.12.1
python-dotenv==1.2.2
gunicorn==22.0.0

# ใช้สร้างภาพ QR code ตอนตั้งค่ายืนยันตัวตนสองชั้น (2FA) — ตัวอัลกอริทึม TOTP เขียนเองด้วย stdlib ล้วน
# ไม่พึ่ง pyotp/qrcode ตรงๆ แต่ยังต้องพึ่ง 2 ไลบรารีนี้สำหรับ "วาดภาพ" QR ให้สแกนได้
reportlab==4.4.10
Pillow==12.2.0

# หมายเหตุ: ไม่รวม psycopg2-binary (สำหรับต่อ PostgreSQL ออนไลน์) ไว้ในไฟล์นี้โดยตั้งใจ เพราะ
# ใช้งานทั่วไปแบบ SQLite ในเครื่อง (ค่าเริ่มต้น) ไม่จำเป็นต้องใช้เลย และบางเครื่อง/บาง Python เวอร์ชัน
# ไม่มี wheel สำเร็จรูปให้ ทำให้ pip พยายาม build จากซอร์สแล้วล้มเหลว (ต้องมี pg_config ของ PostgreSQL)
# ถ้าต้องการต่อฐานข้อมูล PostgreSQL ออนไลน์ ให้ติดตั้งเพิ่มจาก requirements-postgres.txt แยกต่างหาก
# (ดู DEPLOY.md หัวข้อ "ต่อฐานข้อมูลออนไลน์")
