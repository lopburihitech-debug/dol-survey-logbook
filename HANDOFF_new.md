# สถานะงาน — ส่งประกวด กรมที่ดิน (อ่านไฟล์นี้ก่อนถ้าเปิดแชทใหม่)

**เส้นตาย:** เย็น/ค่ำวันอาทิตย์นี้ (ราว 17:00-20:00 น.)

**ต้องส่ง 4 อย่าง:**
1. ระบบขึ้นออนไลน์ใช้งานได้จริง มีลิงก์ให้กรรมการลองเล่น — **สำคัญที่สุด ถ้าเวลาไม่พอให้ทำอันนี้ก่อน**
2. หน้าติดตามงานสำหรับประชาชน
3. อินโฟกราฟิกแบบ One-page (ภาษาไทย)
4. คลิปวิดีโอนำเสนอ 5 นาที

## สิ่งที่ทำเสร็จแล้ว

- ระบบหลักทั้งหมด (จัดการเคสรังวัด, ปฏิทินนัด, ช่างรังวัด, ผู้ใช้/สิทธิ์, 2FA, หมุดหลักเขต+แผนที่, shapefile export ฯลฯ)
- **หน้าติดตามงานสำหรับประชาชน — เสร็จแล้ว:** `frontend/track.html` + API `/api/v1/public/track` — ไม่ต้องล็อกอิน, ยืนยันด้วยเลข รว.19 + เบอร์โทร 4 ตัวท้าย, มี rate limit กันไล่เดา, ทดสอบผ่านแล้ว (ดู `tests/test_public_track.py`)
- เครื่องมือ deploy เตรียมพร้อมหมดแล้ว: `Dockerfile`, `docker-compose.yml`, `DEPLOY.md` (ขั้นตอนละเอียด 3 ทางเลือก: Railway/Render/Fly.io), `backend/requirements.txt`, `backend/requirements-postgres.txt`

## ที่ยังไม่ได้ทำ

- **ยังไม่ได้ deploy ขึ้นออนไลน์จริง** — ดูหัวข้อ "อัปเดตล่าสุด" ด้านล่าง สำคัญมาก อย่าเสียเวลาลองซ้ำสิ่งที่ทดสอบแล้วว่าใช้ไม่ได้
- อินโฟกราฟิก One-page — ยังไม่เริ่ม
- สคริปต์/คลิปวิดีโอนำเสนอ 5 นาที — ยังไม่เริ่ม

## อัปเดตล่าสุด (เซสชันที่เชื่อม Claude Desktop กับเครื่อง com-nu แล้ว)

เชื่อม device bridge สำเร็จแล้ว แต่เจอข้อจำกัดที่ทำให้ **รัน deploy ให้อัตโนมัติทั้งหมดจากในแชทไม่ได้**:

1. เครื่องมือที่มีตอนนี้เชื่อมได้แค่ระดับไฟล์ (อ่าน/เขียนไฟล์ในโฟลเดอร์โปรเจกต์) — **ไม่มีเครื่องมือรันคำสั่ง (shell) บนเครื่อง Windows ของคุณ** เลยสั่ง `railway login` / `railway up` บนเครื่องคุณแทนคุณไม่ได้
2. แซนด์บ็อกซ์ cloud ที่ผมรันอยู่ต่อเน็ตออกไปหา `railway.app` ไม่ได้เลย (ทดสอบแล้ว — connection refused)
3. ลองจะ push โค้ดขึ้น GitHub แทน (แล้วให้คุณกด "Deploy from GitHub" บน Railway dashboard เอง) แต่ GitHub API/OAuth จากแซนด์บ็อกซ์นี้ถูกจำกัดไว้เฉพาะ repo ที่ตั้งค่าไว้ล่วงหน้าเท่านั้น สร้าง repo ใหม่/auth บัญชี GitHub ทั่วไปจากในนี้ไม่ได้
4. Claude in Chrome extension ยังไม่ได้เชื่อมต่อในเซสชันนี้ — เลยขับเบราว์เซอร์จริงของคุณให้คลิก Railway dashboard แทนไม่ได้ด้วย

**สรุป: ทางที่เหลือคือต้องให้คุณเป็นคนรันคำสั่ง/คลิกเองบนเครื่องคุณ** (ผมเตรียมทุกอย่างให้พร้อมที่สุดแล้ว — ไฟล์ deploy, JWT secret ที่สุ่มไว้ให้แล้ว, คำสั่ง copy-paste ครบ)

### วิธี deploy ที่เหลือ — เลือก 1 ใน 2 ทาง

**ทางเลือก A (แนะนำ — เร็วสุด ไม่ต้องพึ่ง GitHub): รัน Railway CLI เองบนเครื่องคุณ**

เปิด PowerShell ที่โฟลเดอร์โปรเจกต์แล้ววางคำสั่งนี้ทีละบรรทัด (JWT secret สุ่มไว้ให้แล้วด้านล่าง — ใช้ได้เลย ไม่ต้องสุ่มเอง):

```powershell
npm install -g @railway/cli
railway login
railway init
railway variables set JWT_SECRET_KEY="b97eae24de14992be22298b16b0f9352c66596e7142799f5098ac8a51bb1487b"
railway variables set DATABASE_PATH="/app/backend/data/dol_survey_logbook.db"
railway volume add --mount-path /app/backend/data
railway up
railway domain
```

`railway login` จะเปิดเบราว์เซอร์ให้ล็อกอิน — ล็อกอินแล้วปิดกลับมาที่ PowerShell ได้เลย `railway domain` ตอนท้ายจะได้ลิงก์สาธารณะ (`https://xxxx.up.railway.app`) เอาไปส่งกรรมการได้ทันที

**ทางเลือก B: เชื่อม Claude in Chrome extension แล้วให้ผมขับเบราว์เซอร์คลิกให้**

ติดตั้ง/เชื่อม Chrome extension ของ Claude (https://claude.ai/chrome) แล้วบอกในแชทว่าเชื่อมแล้ว — ผมจะ push โค้ดขึ้น GitHub ผ่านหน้าเว็บ (github.com upload) แล้วขับเบราว์เซอร์ไปตั้งค่า Railway dashboard ให้ทั้งหมด

### หลัง deploy เสร็จ (ทำทันทีก่อนส่งลิงก์ให้กรรมการ)

1. เข้าไปเปลี่ยนรหัสผ่านบัญชีทดสอบทุกบัญชี (admin/director/supervisor1/surveyor1/surveyor2 — รหัสผ่านเริ่มต้นอยู่ใน `backend/seed.py`)
2. ทดสอบเข้า `/track.html` บนลิงก์จริงว่าใช้งานได้
3. ค่อยไปทำอินโฟกราฟิกกับวิดีโอ (ใช้ลิงก์จริง + QR code ในอินโฟกราฟิกได้เลย)
